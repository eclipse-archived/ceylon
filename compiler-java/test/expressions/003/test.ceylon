void test(Process p) {
    p.writeLine(fib(0));
    p.writeLine(fib(1));
    p.writeLine(fib(2));
    p.writeLine(fib(3));
    p.writeLine(fib(4));
    p.writeLine(fib(5));
    p.writeLine(fib(6));
    p.writeLine(fib(7));
    p.writeLine(fib(8));
    p.writeLine(fib(9));
    p.writeLine(fib(10));
    p.writeLine(fib(11));
    p.writeLine(fib(12));
}
