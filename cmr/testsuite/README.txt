
The "source" folder contains the Ceylon sources for the pre-compiled modules
used in the test suite (see src/test/resources/repo) so they can be compiled
again when necessary (for example when code generation is no longer backward
compatible with the ModuleInfoReaders).
